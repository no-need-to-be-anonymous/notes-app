name: Tests
on: [push, pull_request]
jobs:
   tests:
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@v3
         - run: yarn run docker:up-test
         - if: ${{ failure() }}
           run: exit code 1
